<?php
// Bật hiển thị lỗi trên trình duyệt để dễ kiểm tra
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);

define('DB_HOST', 'localhost');
define('DB_USER', 'ten_database_cua_ban'); // Thay bằng user database thực tế của bạn trên 123host
define('DB_PASS', 'matkhau_database_cua_ban'); // Thay bằng mật khẩu database
define('DB_NAME', 'ten_database_cua_ban'); // Thay bằng tên database

try {
    $pdo = new PDO("mysql:host=" . DB_HOST . ";dbname=" . DB_NAME . ";charset=utf8mb4", DB_USER, DB_PASS);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
} catch (PDOException $e) {
    die("Lỗi kết nối cơ sở dữ liệu: " . $e->getMessage());
}

if (session_status() === PHP_SESSION_NONE) {
    session_start();
}
?>
